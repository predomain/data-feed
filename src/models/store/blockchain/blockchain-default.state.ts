import { BlockchainStateModel } from "./blockchain.state";

export const BlockchainDefaultStateModel = {
  startingBlockNumber: undefined,
  latestBlock: undefined,
  nextBlockNumber: undefined,
} as BlockchainStateModel;
